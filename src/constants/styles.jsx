const buttonStyles =
  "!border !border-gray-300 !border-solid !capitalize !duration-500 !ease-in-out !font-semibold !px-1 !py-1.5 !shadow-none !text-xs !tracking-normal !transition-all !truncate !w-fit";

const styles = { buttonStyles };

export default styles;
